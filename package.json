{
  "name": "skys-discord-bot",
  "version": "1.0.0",
  "description": "",
  "main": "./.out/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc": "tsc",
    "deploy": "forever stopall && tsc && Copy.bat > nul && timeout /t 240 /nobreak && start_bot.bat",
    "deploy-d": "tsc && node ./js/index.js"
  },
  "author": "Dan S",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^13.4.0"
  }
}
